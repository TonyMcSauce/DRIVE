/* DRIVE v0.44 — Vehicle Memory 2.0 */
(()=>{
"use strict";
const esc=v=>String(v??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const fmtKm=v=>Number(v||0).toLocaleString()+" km";
const dateOf=r=>new Date(r?.date||r?.startTime||r?.createdAt||0),sortNewest=(a,b)=>dateOf(b)-dateOf(a),vehicleOnly=(rows,id)=>rows.filter(r=>!id||r.vehicleId==null||r.vehicleId===id);
function card(label,value,detail=""){return `<div class="memory-v044-stat"><span>${esc(label)}</span><strong>${esc(value)}</strong>${detail?`<small>${esc(detail)}</small>`:""}</div>`}
function empty(label,copy){return `<div class="memory-v044-empty"><strong>${esc(label)}</strong><span>${esc(copy)}</span></div>`}
function styles(){if(document.getElementById("driveV044MemoryStyles"))return;const s=document.createElement("style");s.id="driveV044MemoryStyles";s.textContent=`#vehicleMemory{padding:4px 0 24px}.memory-v044-identity{display:flex;justify-content:space-between;gap:14px;align-items:flex-start;margin-bottom:14px}.memory-v044-identity h2{margin:6px 0 4px;font-size:28px;letter-spacing:-.04em}.memory-v044-identity p{margin:0;color:var(--muted);font-size:11px}.memory-v044-edit{min-height:34px;padding:0 11px;border:1px solid var(--line);border-radius:10px;background:var(--surface);color:var(--muted);font-size:8px;font-weight:900;letter-spacing:.08em;cursor:pointer}.memory-v044-grid,.memory-v044-summary{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.memory-v044-stat{padding:13px;border:1px solid var(--line);border-radius:14px;background:var(--surface)}.memory-v044-stat span,.memory-v044-heading span,.memory-v044-state span{display:block;color:var(--muted);font-size:8px;font-weight:900;letter-spacing:.1em}.memory-v044-stat strong{display:block;margin-top:6px;font-size:17px;letter-spacing:-.02em}.memory-v044-stat small{display:block;margin-top:3px;color:var(--muted);font-size:9px;line-height:1.3}.memory-v044-section{margin-top:18px}.memory-v044-heading{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:8px;padding:0 2px}.memory-v044-heading small{color:var(--muted);font-size:8px;font-weight:800}.memory-v044-state{display:grid;gap:7px}.memory-v044-state>div{padding:12px;border:1px solid var(--line);border-radius:13px;background:var(--surface)}.memory-v044-state strong{display:block;margin-top:5px;font-size:11px;line-height:1.4}.memory-v044-timeline{display:grid;gap:6px}.memory-v044-timeline article{display:grid;grid-template-columns:9px minmax(0,1fr) auto;gap:9px;align-items:center;padding:11px 12px;border:1px solid var(--line);border-radius:12px;background:var(--surface)}.memory-v044-timeline article>div{min-width:0}.memory-v044-timeline strong{display:block;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.memory-v044-timeline small{display:block;margin-top:3px;color:var(--muted);font-size:8px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.memory-v044-timeline b{font-size:9px;white-space:nowrap}.memory-v044-dot{width:7px;height:7px;border-radius:50%;background:var(--muted)}.memory-v044-dot.service{background:var(--accent)}.memory-v044-dot.fuel{background:var(--fuel)}.memory-v044-dot.expense{background:var(--warning)}.memory-v044-dot.document{background:var(--blue)}.memory-v044-empty{padding:14px;border:1px dashed var(--line);border-radius:13px}.memory-v044-empty strong{display:block;font-size:11px}.memory-v044-empty span{display:block;margin-top:4px;color:var(--muted);font-size:10px;line-height:1.4}@media(min-width:721px){.memory-v044-grid,.memory-v044-summary{grid-template-columns:repeat(4,minmax(0,1fr))}}`;document.head.appendChild(s)}
async function collect(){const vehicle=typeof getActiveVehicle==="function"?await getActiveVehicle():null;if(!vehicle)return null;const [fuel,trips,maintenance,expenses,documents]=await Promise.all(["fuel","trips","maintenance","expenses","documents"].map(getAllRecords));const id=vehicle.id;return{vehicle,fuel:vehicleOnly(fuel,id),trips:vehicleOnly(trips,id),maintenance:vehicleOnly(maintenance,id),expenses:vehicleOnly(expenses,id),documents:vehicleOnly(documents,id)}}
function ensureRoot(){const car=document.getElementById("carPage");if(!car)return null;let root=document.getElementById("vehicleMemory");if(!root){root=document.createElement("div");root.id="vehicleMemory";car.innerHTML="";car.appendChild(root)}return root}
function render(data){const root=ensureRoot();if(!root||!data)return;const{vehicle,fuel,trips,maintenance,expenses,documents}=data,latestService=[...maintenance].sort(sortNewest)[0],latestDrive=[...trips].sort(sortNewest)[0],fuelSpend=fuel.reduce((s,r)=>s+Number(r.cost||0),0),maintenanceSpend=maintenance.reduce((s,r)=>s+Number(r.cost||0),0),expenseSpend=expenses.reduce((s,r)=>s+Number(r.amount||r.cost||0),0),totalSpend=fuelSpend+maintenanceSpend+expenseSpend,age=Math.max(0,new Date().getFullYear()-Number(vehicle.year||new Date().getFullYear()));const records=[...maintenance.map(r=>({type:"SERVICE",title:r.title||r.name||"Maintenance",meta:r.odometer?fmtKm(r.odometer):"Service record",date:dateOf(r),cost:Number(r.cost||0)})),...fuel.map(r=>({type:"FUEL",title:`${Number(r.litres||0).toFixed(1)} L fuel`,meta:r.odometer?fmtKm(r.odometer):"Fuel entry",date:dateOf(r),cost:Number(r.cost||0)})),...expenses.map(r=>({type:"EXPENSE",title:r.title||r.description||r.category||"Expense",meta:r.odometer?fmtKm(r.odometer):r.category||"Expense",date:dateOf(r),cost:Number(r.amount||r.cost||0)})),...documents.map(r=>({type:"DOCUMENT",title:r.name||r.filename||"Document",meta:"Vehicle document",date:dateOf(r),cost:0}))].sort((a,b)=>b.date-a.date).slice(0,8),issueText=vehicle.knownIssues||vehicle.knownIssue||"No known issues recorded";root.innerHTML=`<section class="memory-v044-identity"><div><span class="eyebrow">VEHICLE MEMORY</span><h2>${esc(vehicle.name||`${vehicle.make||"Vehicle"} ${vehicle.model||""}`)}</h2><p>${esc(vehicle.make||"")} ${esc(vehicle.model||"")} · ${esc(vehicle.year||"—")} · ${esc(vehicle.engine||"Engine not recorded")}</p></div><button type="button" class="memory-v044-edit" id="memoryVehicleEdit">EDIT</button></section><div class="memory-v044-grid">${card("ODOMETER",fmtKm(vehicle.odometer),"Current vehicle reading")}${card("AGE",age+" years",vehicle.year?`Model year ${vehicle.year}`:"Year not recorded")}${card("DRIVES",String(trips.length),"Recorded trips")}${card("SERVICE RECORDS",String(maintenance.length),"Maintenance history")}</div><section class="memory-v044-section"><div class="memory-v044-heading"><span>RECORDED COSTS</span><small>${fuel.length+maintenance.length+expenses.length+documents.length} records</small></div><div class="memory-v044-summary">${card("FUEL SPEND",`P${fuelSpend.toFixed(2)}`,`${fuel.length} fuel entries`)}${card("SERVICE SPEND",`P${maintenanceSpend.toFixed(2)}`,`${maintenance.length} service records`)}${card("TOTAL RECORDED",`P${totalSpend.toFixed(2)}`,"Fuel + service + expenses")}${card("DOCUMENTS",String(documents.length),"Vehicle documents")}</div></section><section class="memory-v044-section"><div class="memory-v044-heading"><span>KNOWN STATE</span></div><div class="memory-v044-state"><div><span>KNOWN ISSUES</span><strong>${esc(issueText)}</strong></div><div><span>LAST SERVICE</span><strong>${latestService?esc(latestService.title||latestService.name||"Maintenance")+" · "+esc(latestService.odometer?fmtKm(latestService.odometer):dateOf(latestService).toLocaleDateString()):"No service recorded yet"}</strong></div><div><span>LAST DRIVE</span><strong>${latestDrive?esc(Number(latestDrive.distance||0).toFixed(1)+" km")+" · "+dateOf(latestDrive).toLocaleDateString():"No drive recorded yet"}</strong></div></div></section><section class="memory-v044-section"><div class="memory-v044-heading"><span>MEMORY TIMELINE</span><small>Latest records</small></div><div class="memory-v044-timeline">${records.length?records.map(r=>`<article><span class="memory-v044-dot ${r.type.toLowerCase()}"></span><div><strong>${esc(r.title)}</strong><small>${esc(r.type)} · ${esc(r.meta)} · ${r.date.toLocaleDateString()}</small></div>${r.cost?`<b>P${r.cost.toFixed(2)}</b>`:""}</article>`).join(""):empty("Nothing recorded yet","Your fuel, service, expense and document history will build this memory automatically.")}</div></section>`;document.getElementById("memoryVehicleEdit")?.addEventListener("click",()=>window.DRIVE_VEHICLE_SETTINGS?.open?.());
const car=document.getElementById("carPage");if(car){let mount=document.getElementById("carComponentsMount");if(!mount){mount=document.createElement("div");mount.id="carComponentsMount";car.appendChild(mount)}window.DRIVE_COMPONENTS?.refresh?.()}}
async function refresh(){try{styles();const data=await collect();if(data)render(data)}catch(error){console.error("Vehicle Memory failed",error)}}
async function open(){if(typeof window.DRIVE_APP?.showPage==="function")window.DRIVE_APP.showPage("carPage");await refresh()}
window.DRIVE_MEMORY={refresh,open};if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",refresh,{once:true});else refresh();
})();
