/* DRIVE v0.9 — Maintenance, Expenses & Service Intelligence */
(() => {
"use strict";
const $=id=>document.getElementById(id), today=()=>new Date().toISOString().split("T")[0];
const LIM={odometer:99999999,cost:1000000,title:120,description:500,intervalKm:500000,intervalMonths:120};
const dateOk=v=>/^\d{4}-\d{2}-\d{2}$/.test(v), moneyOk=v=>Number.isFinite(v)&&v>=0&&v<=LIM.cost, odoOk=v=>Number.isFinite(v)&&v>=0&&v<=LIM.odometer;
function same(r,v){return !v?.id||r.vehicleId==null||r.vehicleId===v.id} function dateOf(r){return new Date(r?.date||r?.createdAt||0)}
function modal(id,title,eyebrow,body){if($(id))return $(id);const el=document.createElement("div");el.id=id;el.className="modal hidden";el.setAttribute("role","dialog");el.setAttribute("aria-modal","true");el.innerHTML=`<div class="modal-sheet"><div class="modal-handle" aria-hidden="true"></div><div class="modal-header"><div><span class="eyebrow">${eyebrow}</span><h2 id="${id}-title">${title}</h2></div><button type="button" class="modal-close" data-close="${id}" aria-label="Close ${title.toLowerCase()}">×</button></div>${body}</div>`;el.setAttribute("aria-labelledby",`${id}-title`);document.body.appendChild(el);el.addEventListener("click",e=>{if(e.target===el)el.classList.add("hidden")});el.querySelector("[data-close]").addEventListener("click",()=>el.classList.add("hidden"));return el}
function ensureUI(){modal("maintenanceModal","Maintenance","SERVICE RECORD",`<form id="maintenanceForm" novalidate><label for="maintenanceTitle">Service / repair<input id="maintenanceTitle" maxlength="120" type="text" placeholder="Engine oil + filter" required></label><label for="maintenanceDescription">Description<input id="maintenanceDescription" maxlength="500" type="text" placeholder="What was done?"></label><label for="maintenanceOdometer">Odometer<div class="input-unit"><input id="maintenanceOdometer" type="number" inputmode="decimal" min="0" max="99999999" step="1" placeholder="128421" required><span aria-hidden="true">KM</span></div></label><label for="maintenanceCost">Cost<div class="input-unit"><span aria-hidden="true">P</span><input id="maintenanceCost" type="number" inputmode="decimal" min="0" max="1000000" step="0.01" placeholder="1240"></div></label><label for="maintenanceDate">Date<input id="maintenanceDate" type="date" required></label><div class="form-section-label">NEXT SERVICE</div><label for="maintenanceIntervalKm">Service interval<div class="input-unit"><input id="maintenanceIntervalKm" type="number" inputmode="decimal" min="0" max="500000" step="1000" placeholder="10000"><span aria-hidden="true">KM</span></div></label><label for="maintenanceIntervalMonths">Time interval<div class="input-unit"><input id="maintenanceIntervalMonths" type="number" inputmode="numeric" min="0" max="120" step="1" placeholder="6"><span aria-hidden="true">MONTHS</span></div></label><p class="form-hint">Leave an interval blank if this work has no scheduled repeat.</p><p id="maintenanceError" class="form-error" role="alert" hidden></p><button class="submit-button" type="submit">SAVE SERVICE RECORD</button></form>`);modal("expenseModal","Expense","VEHICLE COST",`<form id="expenseForm" novalidate><label for="expenseCategory">Category<select id="expenseCategory"><option>Parts</option><option>Tyres</option><option>Repairs</option><option>Insurance</option><option>Licensing</option><option>Other</option></select></label><label for="expenseDescription">Description<input id="expenseDescription" maxlength="500" type="text" placeholder="What did you spend on?"></label><label for="expenseAmount">Amount<div class="input-unit"><span aria-hidden="true">P</span><input id="expenseAmount" type="number" inputmode="decimal" min="0" max="1000000" step="0.01" placeholder="500" required></div></label><label for="expenseOdometer">Odometer<div class="input-unit"><input id="expenseOdometer" type="number" inputmode="decimal" min="0" max="99999999" step="1"><span aria-hidden="true">KM</span></div></label><label for="expenseDate">Date<input id="expenseDate" type="date" required></label><p id="expenseError" class="form-error" role="alert" hidden></p><button class="submit-button" type="submit">SAVE EXPENSE</button></form>`)}
async function vehicle(){return typeof getActiveVehicle==="function"?getActiveVehicle():null}
async function intelligence(){const v=await vehicle(),list=(await getAllRecords("maintenance")).filter(r=>same(r,v)).sort((a,b)=>dateOf(b)-dateOf(a)),now=new Date(),odo=Number(v?.odometer||0),scheduled=list.filter(r=>Number(r.intervalKm||0)>0||Number(r.intervalMonths||0)>0),latestScheduled=scheduled[0]||null;let nextOdo=null,nextDate=null;if(latestScheduled&&Number(latestScheduled.intervalKm)>0)nextOdo=Number(latestScheduled.odometer||0)+Number(latestScheduled.intervalKm);if(latestScheduled&&Number(latestScheduled.intervalMonths)>0){const d=dateOf(latestScheduled);d.setMonth(d.getMonth()+Number(latestScheduled.intervalMonths));nextDate=d}const odoDue=nextOdo!=null&&odo>=nextOdo,dateDue=nextDate!=null&&now>=nextDate,km=nextOdo!=null?nextOdo-odo:null,days=nextDate!=null?Math.ceil((nextDate-now)/86400000):null,soon=!odoDue&&!dateDue&&((km!=null&&km<=1000)||(days!=null&&days<=30));return{vehicle:v,records:list,latest:list[0]||null,latestScheduled,nextServiceOdometer:nextOdo,nextServiceDate:nextDate,kmRemaining:km,daysRemaining:days,status:!latestScheduled?(list.length?"baseline":"unknown"):(odoDue||dateDue?"overdue":soon?"due-soon":"ok")}}
async function refresh(){const v=await vehicle(),filter=a=>a.filter(r=>same(r,v));window.DRIVE_MAINTENANCE.lastMaintenance=filter(await getAllRecords("maintenance")).sort((a,b)=>dateOf(b)-dateOf(a));window.DRIVE_MAINTENANCE.lastExpenses=filter(await getAllRecords("expenses")).sort((a,b)=>dateOf(b)-dateOf(a));window.DRIVE_MAINTENANCE.serviceIntelligence=await intelligence();if(typeof renderRecentActivity==="function")await renderRecentActivity();window.DRIVE_MAINTENANCE_INTEL?.refresh()}
function setup(){ensureUI();["maintenanceDate","expenseDate"].forEach(id=>{if($(id))$(id).value=today()});$("maintenanceForm").addEventListener("submit",async e=>{e.preventDefault();const v=await vehicle(),title=$("maintenanceTitle").value.trim(),description=$("maintenanceDescription").value.trim(),odometer=Number($("maintenanceOdometer").value||0),cost=Number($("maintenanceCost").value||0),date=$("maintenanceDate").value,intervalKm=Number($("maintenanceIntervalKm").value||0),intervalMonths=Number($("maintenanceIntervalMonths").value||0),error=$("maintenanceError");if(!title||title.length>LIM.title||description.length>LIM.description||!odoOk(odometer)||!moneyOk(cost)||!dateOk(date)||intervalKm<0||intervalKm>LIM.intervalKm||intervalMonths<0||intervalMonths>LIM.intervalMonths){error.textContent="Check the service details and try again.";error.hidden=false;return}error.hidden=true;let nextServiceDate=null;if(intervalMonths>0){const d=new Date(`${date}T12:00:00`);d.setMonth(d.getMonth()+intervalMonths);nextServiceDate=d.toISOString().split("T")[0]}await addRecord("maintenance",{vehicleId:v?.id,title,description,odometer,cost,date,intervalKm,intervalMonths,nextServiceOdometer:intervalKm>0?odometer+intervalKm:null,nextServiceDate,createdAt:new Date().toISOString(),synced:false});e.target.reset();$("maintenanceDate").value=today();$("maintenanceModal").classList.add("hidden");await refresh();window.DRIVE_V07?.refresh();window.dispatchEvent(new CustomEvent("drive:datachanged",{detail:{store:"maintenance"}}))});$("expenseForm").addEventListener("submit",async e=>{e.preventDefault();const v=await vehicle(),category=$("expenseCategory").value,description=$("expenseDescription").value.trim(),amount=Number($("expenseAmount").value||0),odometer=Number($("expenseOdometer").value||0),date=$("expenseDate").value,error=$("expenseError");if(!description||description.length>LIM.description||!moneyOk(amount)||!odoOk(odometer)||!dateOk(date)){error.textContent="Check the expense details and try again.";error.hidden=false;return}error.hidden=true;await addRecord("expenses",{vehicleId:v?.id,category,description,amount,odometer,date,createdAt:new Date().toISOString(),synced:false});e.target.reset();$("expenseDate").value=today();$("expenseModal").classList.add("hidden");await refresh();window.DRIVE_V07?.refresh();window.dispatchEvent(new CustomEvent("drive:datachanged",{detail:{store:"expenses"}}))});const buttons=[...document.querySelectorAll("#morePage .settings-list button")],find=label=>buttons.find(b=>b.textContent.trim().toLowerCase().startsWith(label));find("maintenance")?.addEventListener("click",()=>{$("maintenanceModal").classList.remove("hidden");$("maintenanceTitle")?.focus()});find("expenses")?.addEventListener("click",()=>{$("expenseModal").classList.remove("hidden");$("expenseCategory")?.focus()});document.querySelector('[data-action="service"]')?.addEventListener("click",()=>{$("maintenanceModal").classList.remove("hidden");$("maintenanceTitle")?.focus()});window.DRIVE_MAINTENANCE={refresh,intelligence,openMaintenance:()=>{$("maintenanceModal").classList.remove("hidden");$("maintenanceTitle")?.focus()},lastMaintenance:[],lastExpenses:[],serviceIntelligence:null};refresh()}
document.addEventListener("DOMContentLoaded",setup);
})();
